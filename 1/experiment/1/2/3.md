## 原代码分析

其中使用双重循环+`std::set`,带来O($n^2$)的复杂度

## 优化

可以使用哈希表

思路：

1.  统计频率：首先遍历一遍数组，使用哈希表记录每个元素出现的总次数
2.  按序输出：再次按顺序遍历数组.如果当前元素的出现总次数大于 1，并且之前没有输出过（可以使用另一个哈希集合 `printed` 来记录已输出元素），则输出该元素并将其标记为已输出.这一步的时间复杂度也是 $O(N)$

这样时间复杂度就是O($n$)
